<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.OrderDetailMapper">

    <insert id="save">
        insert into order_detail(name, image, order_id, dish_id, setmeal_id, dish_flavor, number, amount) VALUES
        <foreach collection="orderDetails" item="item" separator=",">
            (#{item.name}, #{item.image}, #{item.orderId}, #{item.dishId}, #{item.setmealId}, #{item.dishFlavor}, #{item.number}, #{item.amount})
        </foreach>

    </insert>
    <select id="getById" resultType="com.sky.entity.OrderDetail">
        select * from order_detail where order_id = #{id}
    </select>
    <select id="top" resultType="com.sky.dto.GoodsSalesDTO">
        select name, sum(order_detail.number) as number from order_detail left join orders on order_id = orders.id where order_time between #{begin} and #{end} group by order_detail.name order by sum(order_detail.number) desc
    </select>
</mapper>